# MGA Connect - Backend

The server-side API for MGA Connect, built with **Node.js** and **Express**.

## 🚀 Setup
1. **Install dependencies:** `npm install`
2. **Configure Environment:** Create a `.env` file with your `PORT` and `MONGO_URI`.
3. **Start the server:** `node server.js`

## 📁 Key Files
* `server.js`: Main entry point.
* `BookingModel.js`: Database schema.
* `populateBookingCodes.js`: Script for database initialization.